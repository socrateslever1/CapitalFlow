Verification completed against production Supabase after the hardening migrations. Legacy ID-only portal overloads no longer allow anon/authenticated execution. Current token + shortcode portal overloads remain executable for the portal flow. Anonymous execution remains disabled for agreement schedule editing and source deletion/archive, while authenticated execution is preserved. Admin password reset remains executable by authenticated users but now enforces access-level-1 authorization inside the function.